post:
  tags:
    - Reporting
  summary: Retrieve CSV statements report
  description: TODO

  parameters:
    TODO

  requestBody:
    content:
      application/json:
        schema:
          $ref: 'TODO'
  responses:
    '201':
      description: TODO
      content:
        application/json:
          schema:
            $ref: 'TODO'
      headers:
        Cko-Request-Id:
          $ref: "#/components/headers/Cko-Request-Id"
        Cko-Version:
          $ref: "#/components/headers/Cko-Version"
    '422':
      description: Invalid data was sent
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ValidationError'
    '502':
      description: Bad gateway
